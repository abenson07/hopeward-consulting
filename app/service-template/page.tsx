import { HopewardPlaceholderPage, hopewardPageMetadata } from "@/lib/hopeward-page";

export const metadata = hopewardPageMetadata("service-template");

export default function Page() {
  return <HopewardPlaceholderPage slug="service-template" />;
}
